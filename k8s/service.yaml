apiVersion: v1
kind: Service
metadata:
  name: hevy-mcp
  namespace: mcp
  labels:
    app.kubernetes.io/name: hevy-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hevy-mcp
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
